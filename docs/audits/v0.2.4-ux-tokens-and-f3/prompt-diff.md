# M-1 prompt-diff (target conservador, ~27% recorte)

> Auditoría base: `docs/audits/v0.2.2-comprehensive-audit.md` §7-bis.
> Pre-requisito M-1a: `src/broker/handlers.ts` — `buildSaveResumePrompt`
> ahora prepende el protocolo entero al mensaje `[system:save-resume]`,
> hecho en commit `bc94af9`.
>
> **PAUSA**: este diff debe ser revisado por el usuario antes de
> aplicar el commit M-1b en `src/server/index.ts`.

---

## Conteo medido (4 chars/tok aprox)

| Prompt | chars | tokens (aprox) |
|---|---|---|
| Antes (`buildInstructions("Turing", "backend")`, head ee91c5f) | **7,685** | **~1,922** |
| Después (target conservador) | **6,157** | **~1,540** |
| Δ | **−1,528 chars** | **~−382 tokens** (~20%) |

> El audit (§7-bis) tomaba como referencia **el system prompt aislado**
> (1,920 tok pre-M-1, target conservador ~1,400). El recorte real
> alcanzado aquí (~382 tok) está debajo del audit-target (~520) porque
> conservamos A5/A6/G2/U4b — el audit-target los habría colapsado pero
> requiere eval-harness primero (v0.3.0). Lo documenta `decisions.md`
> (M-1c).

Comando para reproducir conteo:
```sh
git show <commit>:src/server/index.ts | node -e "
const fs=require('fs');
const src=fs.readFileSync(0,'utf-8');
const m=src.match(/buildInstructions[\\s\\S]+?return \\\`([\\s\\S]+?)\\\`;/);
const tmpl=m[1].replace(/\\\${name}/g,'Turing').replace(/\\\${role}/g,'backend');
console.log('chars:',tmpl.length,'tokens (4ch):',Math.ceil(tmpl.length/4));
"
```

---

## Cambios aplicados

### (a) Borrar `## Your tools` lista — líneas 65-71 (~80 tok)

> §7-bis: "Pura redundancia. El MCP host inyecta el catálogo de tools
> con descriptions automáticamente. Esta lista no enseña nada nuevo al
> modelo — es decoración para el lector humano."

**Eliminado:**
```
## Your tools
- list_peers / whoami: see the team and your identity
- send_message / send_to_role: communicate with agents
- check_messages: read incoming messages
- get_history / get_thread_context: conversation context
- set_shared / get_shared / list_shared: shared state by namespace
- set_summary / set_role: update your status
```

### (b) Merge A4 + G6 → una sola regla (~110 tok)

> §7-bis: "A4 + G6 son redundantes garantizado (mismo commit `6da97c6`,
> dicen lo mismo: 'agent-to-agent es silencioso desde la perspectiva
> del user')."

**Antes:**
```
A4. When you receive a message from another AGENT, always respond to THAT agent — never directly to the user. The agent who is talking to the user is the one who summarizes. When you receive a message from another agent and need to respond, respond ONLY to that agent. Do NOT also send a message to the user saying "me preguntaron X". The coordination thread in the dashboard already shows the user what happened.
…
G6. When you coordinate with another agent (send_message + wait for reply), the ENTIRE coordination is silent from the user's perspective. The user only sees: (a) the coordination thread (auto-generated by the dashboard), and (b) your final consolidated response. Nothing in between.
```

**Después** (A4 reescrita, G6 eliminada):
```
A4. Agent-to-agent coordination is silent from the user. When another agent messages you, reply ONLY to that agent — never echo the request to the user. The dashboard's coordination thread is what surfaces the exchange.
```

### (c) Reescribir A2 sin lista en español (~25 tok)

> §7-bis: "Mantener el spirit, fraseo lenguaje-agnóstico." (M-9)

**Antes:**
```
A2. Respond with what they need and STOP. No "gracias", no "perfecto", no "aquí estoy", no "avísame si necesitas algo". Just the answer. If you need more info to complete the task, ask — but only if you genuinely need it.
```

**Después:**
```
A2. Respond with what they need and STOP. No filler acknowledgements ("thanks", "sure thing", "let me know if…"). Just the answer. If you genuinely need more info to complete the task, ask — but only then.
```

### (d) Colapsar G9 → puntero corto (~150 tok, depende de M-1a)

> §7-bis: "G9 (~175 tokens) sólo dispara una vez por sesión pero está
> siempre en contexto. Mover a inyección dinámica (broker prepende
> cuando dispara) recupera tokens sin perder funcionalidad."

**Antes:**
```
G9. When you receive a message starting with "[system:save-resume]", it's the broker asking you to persist a snapshot of your current mental state so you can pick it up next session. Do the following SILENTLY (do NOT reply to the user, do NOT message other agents, do NOT narrate what you're doing):
  - Call set_shared("resume", "<your role>", <value>) where <value> is a JSON string with exactly these fields: { "summary": "1-2 sentences about what you were working on right now", "next_steps": ["short bullet", "short bullet"], "open_questions": ["optional, anything you were waiting on"], "updated_at": "<iso timestamp>" }.
  - Keep summary and bullets SHORT — this gets prepended to your next session's first prompt, so less is more.
  - After calling set_shared, return to whatever you were doing before this message. Do not continue to process the [system:save-resume] as a real task — it's a meta-instruction.
```

**Después:**
```
G9. If you receive a message starting with "[system:save-resume]", the broker has prepended the full protocol to that message — follow the instructions in the message body. Do it silently and return to your prior work.
```

> Confirmación de que G9 sigue funcionando: M-1a (commit bc94af9) hace
> que `handleSaveResume` y `handleProjectDown` pasen por
> `buildSaveResumePrompt(role, now, kind)`, que produce el set_shared
> exacto + JSON shape + "do this silently" en el cuerpo del mensaje.
> El test E2E que cubre la cadena queda agendado en M-1b commit
> (`tests/server/instructions.test.ts` — "snapshot longitud <1500 tok
> + verifica G9 trigger end-to-end con inyección dinámica").

---

## Lo que NO se elimina (riesgos no aceptados)

| Regla | ¿Tocada? | Razón |
|---|---|---|
| A1 (be compact, JSON, no markdown) | No | Sigue siendo única — A2 mira filler, A1 mira formato |
| A3 (publish to shared on schema asks) | No | No duplicado |
| A5 (no refusal) | No | §7-bis: "refuerzo intencional débil" — agresivo lo borraría tras eval; conservador lo mantiene |
| A6 (just do it) | No | Mismo grupo de A5 — mantener hasta tener eval-harness (v0.3.0) |
| U1-U6 / U4b | No | Off-scope. U4b ataca síntoma distinto (verbose hacia user) |
| G1-G8 | No | No duplicados con A* |

---

## Validación

- [x] Tests verdes después de M-1a (476/476)
- [x] Test snapshot `tests/server/instructions.test.ts`:
  - `prompt.length < 6500` (~1,625 tokens — margen sobre 1,540 medidos)
  - NO contiene `## Your tools`
  - A4 menciona "silent" y G6 ya no existe
  - A2 sin lista en español; menciona "filler"
  - G9 colapsado a una línea; NO contiene `set_shared("resume"` ni `next_steps`
- [x] Test E2E `buildSaveResumePrompt` cubre las 4 keys obligatorias
  (summary, next_steps, open_questions, updated_at) + asks-silently +
  embeds (role, timestamp).

---

## Estado: aplicado

Commit M-1b (server/index.ts + tests/server/instructions.test.ts +
export buildSaveResumePrompt) aplica el recorte. Diff y conteos
quedan en este archivo como referencia post-cambio.
